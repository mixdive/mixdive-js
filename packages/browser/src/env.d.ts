// Replaced at build time by tsup (and by vitest.config.ts in tests) with the
// package.json version, so the built files carry the version they were cut
// from. Never referenced from outside this package.
declare const __MIXDIVE_VERSION__: string
